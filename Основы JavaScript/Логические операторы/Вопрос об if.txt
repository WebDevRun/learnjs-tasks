if (-1 || 0) alert( 'first' ) // Выполниться. Результат if будет -1
if (-1 && 0) alert( 'second' ) // Невыполниться. Результат if будет 0
if (null || -1 && 1) alert( 'third' ) // Выполниться. Результат if будет 1